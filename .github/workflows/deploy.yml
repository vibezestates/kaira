name: Deploy Kaira

on:
  push:
    branches: [ "main" ]   # 🔥 Auto deploy when pushing to MASTER branch

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v3

    - name: Deploy to VPS
      uses: appleboy/ssh-action@v1.0.3
      with:
        host: 93.127.195.157
        username: root
        key: ${{ secrets.VPS_KEY }}    # private key from /root/.ssh/id_rsa_kaira
        script: |
          echo "🔁 Deploying Kaira..."
          bash /root/deploy-kaira.sh
